import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'user_interface',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
